version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .vercel/output
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
